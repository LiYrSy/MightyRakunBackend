public record Habit(string id,
                    string icon,
                    string title,
                    string description,
                    string color,
                    bool isCompleted
            ); 